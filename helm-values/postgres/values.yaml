auth:
  postgresPassword: postgres123
  username: backstage
  password: backstage123
  database: backstage

primary:
  persistence:
    enabled: true
    size: 5Gi

  service:
    type: ClusterIP

  resources:
    requests:
      cpu: 250m
      memory: 512Mi

    limits:
      cpu: "1"
      memory: 1Gi
